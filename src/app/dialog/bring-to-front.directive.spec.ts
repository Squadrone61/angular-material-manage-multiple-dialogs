import { BringToFrontDirective } from './bring-to-front.directive';

describe('BringToFrontDirective', () => {
  it('should create an instance', () => {
    const directive = new BringToFrontDirective();
    expect(directive).toBeTruthy();
  });
});
